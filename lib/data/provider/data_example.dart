import 'package:flutter/widgets.dart';

class DataExampleModel extends ChangeNotifier {
  // Use notifyListeners() when something changes
  // To put the Provider in the tree use: ChangeNotifierProvider(create: (context) => ..., child: ...)
  //or MultiProvider()
  // To consume the provided value: Consumer<DataExampleModel>(
  // builder: (context, cart, child) {
  //   return Text(...);
  // },
  // DOCUMENTATION: https://pub.dev/packages/provider

}
